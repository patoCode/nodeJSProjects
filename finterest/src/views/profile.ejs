<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>Profile <%=image.title%></title>

    <%- include('partials/head') %>

</head>
<body>
	<%- include('partials/menu') %>
	<div class="container">
		<div class="col s6 m6 l6 animated zoomInUp z-depth-3">
			<div class="card">
				<div class="card-image">
					<img src="<%= image.path%>">
					<span class="card-title"><%= image.title%></span>
				</div>
				<div class="card-content">
					<p><%=image.description%> - <%= format(image.created_at)%></p>
				</div>
			</div>
		</div>
	</div>
	<div class="fixed-action-btn">
		<a href="/" class="btn-floating btn-large blue-grey darken-3">
			<i class="material-icons left">apps</i>
			Eliminar
		</a>
		<a href="/image/<%= image._id%>/delete" class="btn-floating btn-small red">
			<i class="material-icons left">delete</i>
			Eliminar
		</a>
	</div>
</body>